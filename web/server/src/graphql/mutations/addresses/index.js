var addAddress = require('./add').add;
var removeAddress = require('./remove').remove;
var updateAddress = require('./update').update;

module.exports = {
    addAddress,
    removeAddress,
    updateAddress
}